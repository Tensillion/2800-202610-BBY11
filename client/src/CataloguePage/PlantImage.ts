/**
 * Defines the type Plant, centralizing the logic to be called in required functions.
 * This is the type used when rendering data from the database.
 *
 * @author Umanga Bajgai
 */
export type PlantImage = {
  url: string;
  creator?: string;
  license?: string;
};
